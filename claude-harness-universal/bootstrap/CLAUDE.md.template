# CLAUDE.md — {{專案名稱}}
<!-- 填寫指引:專案的正式名稱,與 repo 名一致。 -->

## 本文件目的

本文件是 {{專案名稱}} 的執行模型(如 Opus)工作守則母版,由 claude-harness-universal 的 `bootstrap/CLAUDE.md.template` 生成。
它分成兩部分:「固定工作流」(來自 harness 協議,**不得因專案需要而修改或跳過**)與「專案變數」(每個專案填自己的值)。

## 如何使用本文件

1. 新專案啟動時,依 `bootstrap/BOOTSTRAP_PROMPT.md` 的程序,把本文件所有 `{{變數}}` 空位填成專案實際值,存為專案根目錄的 `CLAUDE.md`。
2. 填不出來的變數不可留空猜填——列入「待確認問題」問使用者(見 PLANNING.md 階段 1)。
3. 執行模型每次會話開始時讀取本文件;固定工作流的每一步都是強制的。

---

## 專案概要(專案變數)

| 欄位 | 值 |
|---|---|
| 專案名稱 | {{專案名稱}} |
| 一句話目標 | {{一句話目標}} <!-- 填寫指引:這個專案成功時,為誰解決了什麼問題,一句話。 --> |
| 技術棧 | {{技術棧}} <!-- 填寫指引:語言、框架、主要依賴與版本,例如 Python 3.12 + FastAPI + PostgreSQL 16。 --> |
| 建置指令 | {{建置指令}} <!-- 填寫指引:一行可執行的指令,例如 make build 或 npm run build;無建置步驟則填「無」。 --> |
| 測試指令 | {{測試指令}} <!-- 填寫指引:一行可執行的指令,例如 pytest tests/ -x;並註明全量測試的大約耗時。 --> |
| 驗證指令 | {{驗證指令}} <!-- 填寫指引:交付前的總驗證入口,預設為 ./verify.sh(由 templates/verify.sh 生成)。 --> |

## 程式風格與慣例(專案變數)

- {{程式風格要點}} <!-- 填寫指引:條列 3-8 條本專案的硬性慣例:縮排、命名、目錄結構、commit 訊息格式、lint 工具與設定檔位置。以「既有程式碼實際的樣子」為準,不要抄通用教條。 -->

## 領域特別注意事項(專案變數)

- {{領域特別注意事項}} <!-- 填寫指引:條列本專案領域的地雷與紅線:不可動的模組、對外相容性承諾、資料敏感度、法規要求、已知的脆弱區域。沒有就填「無」。 -->

---

## 固定工作流(harness 協議;不可修改、不可跳過)

以下流程引用的協議文件位於 `{{harness路徑}}`(<!-- 填寫指引:harness 文件在本專案中的相對路徑,例如 docs/harness/ 或 ../claude-harness-universal/ -->)。
**若本專案未附這些文件,先向使用者索取 claude-harness-universal 套件,再開始任何非瑣碎任務。**

### 任務執行流程

任何非瑣碎任務,依序執行,前一步完成前不得進入下一步:

1. **規劃**:讀 `PLANNING.md`,依八階段(需求澄清 → 問題重述 → 範圍界定 → 方案枚舉與取捨 → 分解 → 定義驗收 → 執行 → 自審)推進;進入「執行」前,完整填寫 PLANNING.md 文末的「規劃輸出模板」。填不出來的欄位=對應階段沒做完,回去補。
2. **判定任務原型**:在「問題重述」階段標出原型(software-implementation / system-design / research-analysis / technical-writing / debugging / data-analysis),並細讀 `ANTIPATTERNS.md` 中對應原型的一節。
3. **執行中的決策紀律**:任何會改變狀態的動作,先依 `DECISION_PROTOCOL.md` 第 2 節分級(高/中/低風險)並執行對應必要動作;命中第 3 節任一「必須停下來問使用者」觸發條件時,立刻用【升級提問】格式提問,不得先做再說。變更依第 5 節「小步原則」切步:每步=改動 → 驗證 → 記錄。
4. **交付前自審**:依 `GENERAL_RUBRIC.md` 的〈交付前自評指令〉逐維度(R1-R8)自評並附證據;**任一維度低於 3 分,禁止交付,修正後對全部八維度重評**。同時對照 `ANTIPATTERNS.md` 對應原型節逐條檢查,阻斷級反模式必須為零。
5. **驗證**:執行 `{{驗證指令}}`,全部通過(PASS)才算完成。驗證失敗時修到通過,或依 DECISION_PROTOCOL 觸發條件 #9 升級回報;不得弱化驗證標準來換取通過。
6. **交付聲明**:交付時附上:驗收標準逐條結果、Rubric 自評表、「已驗證 / 未驗證」兩清單(格式見 DECISION_PROTOCOL.md 第 4.4 節)。

### 輕量模式(與 PLANNING.md 一致)

同時滿足以下**全部**條件的任務,可用 PLANNING.md 的輕量模式(任務重述 → 驗收標準 → 執行與自審,三步不可再減):

- 小:單次專注可完成,改動不超過 1-2 個檔案或一份短文件。
- 可逆:低成本可復原,且不命中 PLANNING.md 階段 1 表 A 任何「必須先問」情況。
- 範圍明確:只有一種合理解讀,驗收標準顯而易見或已由使用者給定。

任一條不滿足或拿不準,走完整八階段。輕量模式執行中命中 PLANNING.md 的升級規則,立即升級為完整流程。

### 專案文件的維護義務

- 規格變更 → 同步更新 `SPEC.md`;任務進度 → 即時更新 `tasks.md` 狀態欄。
- 命中 `ADR 觸發清單`(見 templates/ADR_TEMPLATE.md)的決定 → 當下補一筆 ADR,不事後補記憶。
- 交付前 → 走一遍 `CHECKLIST.md`,逐格勾選,不留空格。
